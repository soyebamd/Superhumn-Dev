{{ 'section-image-banner.css' | asset_url | stylesheet_tag }}

{%- if section.settings.image_height == 'adapt' and section.settings.image != blank -%}
  {%- style -%}
    @media screen and (max-width: 749px) {
      #Banner-{{ section.id }}::before,
      #Banner-{{ section.id }} .banner__media::before,
      #Banner-{{ section.id }}:not(.banner--mobile-bottom) .banner__content::before {
        padding-bottom: {{ 1 | divided_by: section.settings.image.aspect_ratio | times: 100 }}%;
        content: '';
        display: block;
      }
    }

    @media screen and (min-width: 750px) {
      #Banner-{{ section.id }}::before,
      #Banner-{{ section.id }} .banner__media::before {
        padding-bottom: {{ 1 | divided_by: section.settings.image.aspect_ratio | times: 100 }}%;
        content: '';
        display: block;
      }
    }
  {%- endstyle -%}
{%- endif -%}

{%- style -%}
 


  .superhuman-banner-bg{ 

  background:#000 url('{{  section.settings.image_bg |  image_url: width: 2000  }}');

  
    background-size: 100%;
    height: 100vh;

    position:relative;


  }

  .full-screen{ width:100%; height:100vh;}

  
{%- endstyle -%}


<div
  id="Banner-{{ section.id }}"
  class="{% if settings.animations_reveal_on_scroll %} scroll-trigger animate--fade-in{% endif %}
    

    {%- if section.settings.image_bg != blank -%}
 superhuman-banner-bg
    {% endif %}
   

    full-screen
    
    "
>





</div>


{% schema %}
{
  "name": "SUPERHUMN IMAGE BANNER",
  "tag": "section",
  "class": "section",
  "disabled_on": {
    "groups": ["header", "footer"]
  },
  "settings": [
    {
      "type": "image_picker",
      "id": "image_bg",
      "label": "Add Background Image"
    },
    {
      "type": "image_picker",
      "id": "image_2",
      "label": "Add main hero image"
    }
  ],
  "blocks": [
    
  ],
  "presets": [
    {
      "name": "SUPERHUMN BANNER SECTION"
  
    }
  ]
}
{% endschema %}
