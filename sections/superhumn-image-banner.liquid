{{ 'section-image-banner.css' | asset_url | stylesheet_tag }}

{%- if section.settings.image_height == 'adapt' and section.settings.image != blank -%}
  {%- style -%}
    @media screen and (max-width: 749px) {
      #Banner-{{ section.id }}::before,
      #Banner-{{ section.id }} .banner__media::before,
      #Banner-{{ section.id }}:not(.banner--mobile-bottom) .banner__content::before {
        padding-bottom: {{ 1 | divided_by: section.settings.image.aspect_ratio | times: 100 }}%;
        content: '';
        display: block;
      }
    }

    @media screen and (min-width: 750px) {
      #Banner-{{ section.id }}::before,
      #Banner-{{ section.id }} .banner__media::before {
        padding-bottom: {{ 1 | divided_by: section.settings.image.aspect_ratio | times: 100 }}%;
        content: '';
        display: block;
      }
    }
  {%- endstyle -%}
{%- endif -%}

{%- style -%}
 


  .superhuman-banner-bg{ 

  background-color:#130D09!important;
  background:  url('{{ section.settings.image_bg | image_url: width: 2000 }}') no-repeat, url('{{ '3D_Morph_Lines_12.svg' | file_url }}') no-repeat; 
  background-size: 100%, cover; /* Adjusted background sizes */
  height: 100vh;
  position: relative;
  background-position: center, center; /* Adjusted background positions */
}


.para1{}
  

  .full-screen{ width:100%; height:100vh;}

  .secondImg{ position:absolute; bottom:0; left:0; width:100%;  }

  .secondImg img{ width:100%; height:auto }
  

  
{%- endstyle -%}


<div
  id="Banner-{{ section.id }}"
  class="{% if settings.animations_reveal_on_scroll %} scroll-trigger animate--fade-in{% endif %}
    

    {%- if section.settings.image_bg != blank -%}
 superhuman-banner-bg
    {% endif %}
   

    full-screen
    
    "
>


<p class="para-1">A THOUSAND EATERS <br> CAN'T BE WRONG</p>

<div class="pillu">

<img src="{{'pilu.svg' |  file_url}}" class="pillu">

</div>


<div class="center-box-content center text-center">

<h2 class="titleEffectStrock">ENTER THE</h2>

<h1 class="title highlight">SUPER</h1>

<div class="animation-strock-effect position-relative">
      
    <h3 class="h2 titleEffectStrock">DIMENSION</h3>
    <h3 class="h2 titleEffectStrock">DIMENSION</h3>
    <h3 class="h2 titleEffectStrock">DIMENSION</h3>
  
  </div>

<a href="#" class="btn text-uppercase position-relative mt-40">

<span class="layer-1 ">

<span class="layer-2 ">

Activate
</span>
</span>
</a>


</div>

 {%- if section.settings.image_bg != blank -%}
 <div class="secondImg">
{{ section.settings.image_2 |  image_url: width: 2000 |  image_tag }}

</div>   
{% endif %}


</div>


{% schema %}
{
  "name": "SUPERHUMN IMAGE BANNER",
  "tag": "section",
  "class": "section",
  "disabled_on": {
    "groups": ["header", "footer"]
  },
  "settings": [
    {
      "type": "image_picker",
      "id": "image_bg",
      "label": "Add Background Image"
    },
    {
      "type": "image_picker",
      "id": "image_2",
      "label": "Add main hero image"
    }
  ],
  "blocks": [
    
  ],
  "presets": [
    {
      "name": "SUPERHUMN BANNER SECTION"
  
    }
  ]
}
{% endschema %}
